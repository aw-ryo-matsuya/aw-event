{% extends "AwEventBundle::layout.html.twig" %}

{% block page_title -%}
    {{ 'member.enneagramScreen'|trans }}
{% endblock %}

{% block body -%}
<form action="{{ path('create_enneagram') }}" method="POST">
    <div class="my-table-responsive">
    {% for i in 1..9 %}
        <table class="table table-striped table-bordered table-condensed table-hover">
            <tbody>
            {% for j in 1..20 %}{{ form_widget(_context['type' ~ i ~ 'Form']._token) }}
                <tr>
                    <td>{{ form_widget(_context['type' ~ i ~ 'Form']['question' ~ j]) }}</td>
                    <td>{{ 20 * (i - 1) + loop.index }}</td>
                    <td>{{ ('enneagram.type' ~ i ~ '.question' ~ j)|trans }}</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    {% endfor %}
    </div>
    <section class="button-area">
        <div class="btn-group">
            <button id="clear" type="button" class="btn btn-default btn-sm">{{ 'com_button.clear'|trans }}</a>
            <button type="submit" class="btn btn-primary btn-sm">{{ 'com_button.createOK'|trans }}</bu4tton>
        </div>
    </section>
</form>
<script type="text/javascript">
    $("#clear").click(function() {
        $('input[type="checkbox"]').each(function(){$(this).removeAttr('checked')})
    });
</script>
{% endblock %}
