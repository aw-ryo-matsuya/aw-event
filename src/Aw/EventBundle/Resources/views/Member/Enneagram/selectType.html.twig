{% extends "AwEventBundle::layout.html.twig" %}

{% block page_title -%}
    {{ 'member.selectTypeScreen'|trans }}
{% endblock %}

{% block body -%}
<form action="{{ path('select_type') }}" method="POST">
    {{ form_widget(form.type) }}
    <section class="button-area">
        <div class="btn-group">
            <button type="submit" class="btn btn-primary btn-sm">{{ 'com_button.createOK'|trans }}</bu4tton>
        </div>
    </section>
</form>
{% endblock %}
