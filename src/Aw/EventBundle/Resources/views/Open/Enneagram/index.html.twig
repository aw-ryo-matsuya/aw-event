{% extends "AwEventBundle::layout.html.twig" %}

{% block page_title -%}
    {{ 'top_page.enneagramScreen'|trans }}
{% endblock %}

{% block body -%}
<div class="my-table-responsive">
    <table class="table table-striped table-bordered table-condensed table-hover">
        <tbody>
            {% for i in 1..20 %}
            <tr>
                <td>{{ form_widget(type1Form['question' ~ i]) }}</td>
                <td>{{ loop.index }}</td>
                <td>{{ ('enneagram.type1.question' ~ i)|trans }}</td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
</div>
{% endblock %}
